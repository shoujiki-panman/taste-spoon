# Taste Spoon — プロジェクトルール（CLAUDE.md）

このファイルの内容は毎回必ず守ること。指示と矛盾する場合は確認を取る。

## これは何
「正直パンマン」が実食・正直評価した店から、ユーザー(主に夫婦2人)の味覚プロフィールに
合う店を、ボタンひとつで出すアプリ。ランキングではなく「合う/微妙/合わない」で出す。
"信用できる人の閉じたリスト" から personalize して出すのが価値。

## 絶対ルール(特記がない限り変更禁止)
- calcMatch本体・7軸の意味・判定しきい値は凍結。補正やフィルタは calcMatch の「外側」で足す。
- Design.md を守る。キャラ(正直パンマン)とアニメーションを壊さない。
- 既存コンポーネント(VerdictCardなど)は再利用する。作り直さない。
- 使いやすさ最優先・モバイルファースト。

## 正直さの原則(最重要)
- 「機能が動いて見えるように」データを盛らない/捏造しない(sour5/roast5やニセspiceはNG)。
- 信用は元データの正直さから。未食の店は断定せず「合いそう」と明示し保証と区別。
- 合う店だけでなく微妙/合わないも正直に表示し、注意点を必ず添える。

## データモデル(src/data/stores.json)
- 7軸: bitter/sour/roast/creamy/firstTime/picky/volume(満足感・重み1.5)
- store: id,name,area,genre,signature,axes(7軸),spice(0-4),image,panman,caution,recommend,tip,panmanVerdict
- しきい値(校正済み・凍結): 合う≥70 / 微妙≥56 / 合わない<56
- 辛さ: 2人=spiceCapCouple / 自分だけ=spiceCapSolo。cap超は合うから除外し別枠「⚠辛さ注意」。

## 声(正直パンマン)
- 正直で温かく遊び心。理由は2行(1=合う理由/2=注意点)。合わない時も両面。短く口語。

## 開発フロー
- 方針提示→差分→ユーザーが localhost:5180(HMR)で確認→OKならコミット。
- devサーバはユーザーのターミナルで5180。5173(MulmoClaude本体)は絶対触らない。
- git: ローカルコミットまで。pushはしない。ブランチ feature/verdict-card。

## ロードマップ
- いま: 鉄板モード(「今日どこ行く?」)。次: 冒険モード(未食/老舗/不思議を発見, 味の相性×ワクワク, 関東から)。
- バックエンド章: LLM判定/personalization同期(Firebase等,認証込み)/相談エージェント。
