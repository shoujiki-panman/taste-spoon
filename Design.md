# Taste Spoon — デザインガイドライン

> 「“測る”より“あなたに合うか”」。ランキングじゃない、相性を試食する体験。
> 案内役は **正直パンマン**。正直・温かい・少しズバッと。

このドキュメントは Taste Spoon の見た目・声・ふるまいの基準。
新しい画面やパーツを足すときは、ここのトークンと指針に必ず合わせる。

---

## 1. ブランド & コンセプト

- **コア価値**: 点数や行列ではなく「**あなたの舌に合うか**」を先に知る。
- **トーン**: 正直・温かい・少しズバッと言い切る。忖度しない、でも意地悪じゃない。
- **NG**: 無機質な計測器・分析ダッシュボード感。冷たい青グレーの「データツール」見た目。
- **OK**: 遊び心があるが信頼できる。食欲をそそる暖色。ドット絵レトロ感を“少しだけ”。読みやすくクリーン。

---

## 2. デザイン原則（モバイル前提・使いやすさ最優先）

1. **1画面1アクション** — 各カードは「ひとつのこと」だけ。詰め込まない。
2. **大きめタップ領域** — 触れる要素は最小 **44×44px**。ボタンは全幅。
3. **読みやすさ** — 本文 15px 以上、行間 1.7〜1.8。コントラスト AA 以上。
4. **暖色ベースで統一** — 背景はクリーム。寒色は“キャラの青”などアクセントに限定。
5. **判定は一瞬で伝える** — 色・キャラ・ことばの3点セットで「合う/微妙/合わない」を即理解。
6. **動きは意味があるときだけ** — 登場・状態変化の合図に使う。常時の過剰アニメは避け、`prefers-reduced-motion` を尊重。

---

## 3. カラートークン

CSS カスタムプロパティ名（`--ts-*`）で定義し、全コンポーネントはこれを参照する。

### 背景・サーフェス
| トークン | 値 | 用途 |
|---|---|---|
| `--ts-bg` | `#fdf6e7` | ページ背景（クリーム） |
| `--ts-surface` | `#fffdf7` | カード面 |
| `--ts-surface-2` | `#fffefb` | 入力・判定カード内面 |
| `--ts-surface-warm` | `#fff5e6` | 吹き出し・強調面 |

### テキスト
| トークン | 値 | 用途 |
|---|---|---|
| `--ts-ink` | `#3d2f1e` | 主要テキスト |
| `--ts-ink-body` | `#4a3a26` | 本文 |
| `--ts-ink-sub` | `#6a5236` | 補助ラベル |
| `--ts-ink-muted` | `#9b7d54` | 控えめ・注釈 |

### ブランド
| トークン | 値 | 用途 |
|---|---|---|
| `--ts-red` | `#c0392b` | ブランド赤（見出し・プライマリボタン・スライダー） |
| `--ts-red-ink` | `#a82018` | 赤の濃い影 |
| `--ts-blue` | `#2f6fc4` | キャラの青。アクセント・リンク的用途に限定 |
| `--ts-pan` | `#edc278` | パン（トースト）色。装飾・タグ地に |
| `--ts-crust` | `#b06a2c` | クラスト茶。境界・小見出しアクセント |
| `--ts-accent` | `#d9822b` | 山吹アクセント（キャレット・パンマン名・小バッジ） |

### 境界
| トークン | 値 | 用途 |
|---|---|---|
| `--ts-border` | `#ecdcc0` | 既定のカード枠 |
| `--ts-border-warm` | `#f0d9b5` | 吹き出し枠 |
| `--ts-border-strong` | `#f0c9a0` | 結果カード枠 |

### 判定3色（最重要 / 「一目で違う」へ調整）
現状は 微妙=`#f08c00` と 合わない=`#e8590c` が共にオレンジで**見分けづらい**。
信号機メタファー（緑→黄→赤）で明確に分離する。

| 判定 | トークン | 現状 | **推奨（採用案）** | 補足 |
|---|---|---|---|---|
| 合う | `--ts-fit` | `#2f9e44` | `#2f9e44`（据置） | 緑 |
| 微妙 | `--ts-meh` | `#f08c00` | `#e8a013`（山吹/黄寄せ） | 黄 |
| 合わない | `--ts-no` | `#e8590c` | `#d83a2e`（赤へ寄せ） | 赤＝キャラの赤と同系 |

各色に淡い背景版（`--ts-fit-bg` 等、白に8%混ぜ）を用意し、判定カードの枠＋スコア＋バーに使う。
※ `matchVerdict` のサブラベル（ばっちり/わりと/ちょっと/覚悟）も、この3バンドに内包される色域に収める（文言・閾値は不変）。

---

## 4. タイポグラフィ

| 役割 | フォント | 使用例 |
|---|---|---|
| 表示用（見出し・数字） | **`'Bagel Fat One'`**（丸く太いディスプレイ） | アプリ名 / 判定ラベル / スコア% |
| 本文（日本語対応） | **`'Zen Maru Gothic'`** → `'Hiragino Maru Gothic ProN'`, sans-serif | 説明・コメント・ラベル全般 |

**サイズスケール**（モバイル基準）
| トークン | px | 用途 |
|---|---|---|
| `display` | 34 | アプリタイトル |
| `verdict` | 40 | 判定ラベル（合う等） |
| `h2` | 22 | 料理名（大） |
| `lg` | 18 | 料理名 |
| `body` | 16.5 | パンマンのコメント |
| `base` | 15 | 本文・リード |
| `sm` | 13–14 | ラベル |
| `xs` | 12–12.5 | 注釈 |

行間: 本文 **1.8** / コメント **1.8** / 見出し **1.0–1.2**。

---

## 5. スペーシング・スケール（4の倍数基準）

`4 / 8 / 10 / 12 / 14 / 16 / 18 / 22 / 24`
- カード内パディング: **18px**（標準） / 20px（結果系）
- カード間マージン: **16px**
- 要素間 gap: 8（密）/ 10〜12（標準）/ 14（ゆったり）
- ページ外周: 上24 / 左右18 / 下60

---

## 6. 角丸スケール

| トークン | px | 用途 |
|---|---|---|
| `--ts-radius-sm` | 12 | 入力・ボタン |
| `--ts-radius-md` | 14 | 吹き出し・小カード |
| `--ts-radius-lg` | 18 | カード |
| `--ts-radius-pill` | 999 | バー・チップ |

---

## 7. 影スケール（暖色グレー、軽め）

| トークン | 値 | 用途 |
|---|---|---|
| `--ts-shadow-sm` | `0 3px 10px rgba(160,120,60,.10)` | 吹き出し |
| `--ts-shadow-md` | `0 4px 16px rgba(160,120,60,.08)` | カード |
| `--ts-shadow-lg` | `0 6px 22px rgba(192,57,43,.12)` | 結果カード（赤み） |

影は“浮かせる”ためでなく“紙の温かみ”。濃い黒影は使わない。

---

## 8. コンポーネント指針

### カード
- 面 `--ts-surface`／枠 `2px --ts-border`／角丸 `lg`／影 `md`／パディング 18px。
- 1カード=1目的。見出しは `sm`・太字・`--ts-ink-sub`。

### 判定カード（主役）
- 枠 `3px` を判定色に。中身の順序は固定：**パンマン → 吹き出し → 料理名 → 相性スコア（補助）**。
- 判定ラベルは `verdict`(40px) ＋ 判定色。スコア%は控えめ（20px）。
- 相性バー：トラック `--ts-pan` 系の淡色、フィルは判定色、角丸 pill。

### 吹き出し（speech bubble）
- 面 `--ts-surface-warm`／枠 `2px --ts-border-warm`／角丸 `md`／影 `sm`。
- **しっぽ（三角）で必ずパンマンを指す**。登場は pop、本文はタイプ風（任意）＋点滅キャレット。
- 中身は正直パンマンの“声”。一人称・短く・言い切る。

### プライマリボタン
- 全幅・高さ最低 48px・角丸 `sm`・`--ts-red` 地に白文字・太字。
- 処理中は不透明度を下げ、文言で状態を伝える（例「正直パンマンが味見中…」）。
- タップ時に軽い押し込み（scale .98）。

### スライダー
- `accent-color: --ts-red`。トラック高さは指で扱える太さ。
- 行は「ラベル｜スライダー｜現在値」。値は太字・判定赤。

---

## 9. 正直パンマンの居場所と吹き出し

- **待機（結果前）**: 入力エリア上部で idle 表示（虫眼鏡で立つ⇔指さし）。アプリの案内役。
- **判定時**: 判定カード上部。状態に応じて good / hmm / bad（＋分解中は loading）。
- **吹き出し**: 常にパンマンから出ているように、しっぽで本人を指す。コメント＝パンマンの一言。
- 画像は透過 PNG・`image-rendering: pixelated` でドット感維持。後ろに白い箱を作らない。
- アニメは状態の合図（パラパラ切替／首かしげ／shake／揺れ／登場 pop）。`prefers-reduced-motion` で静止。

---

## 10. 声のトーン（コピーライティング）

- **正直**: 良い悪いをはっきり。「高評価でも自分には合わないことがある」を地で行く。
- **温かい**: 否定で終わらない。「刺さる人にはとことん刺さる」と逃げ道を残す。
- **少しズバッと**: 回りくどくしない。短い言い切り。
- 人称はパンマン視点（見立て・味見）。計測用語（スコア・偏差・係数）を主役にしない。
- 例: ◎「“また食べたい”より“一度確かめる”一皿。」 ／ ✕「総合評価 45 点、標準偏差 0.8。」

---

## 11. 現状の棚卸し（適用時のクリーンアップ対象）

`src/TasteSpoon.jsx` の `S` には、過去の改修で**未使用化したスタイル**が残存：
`dishRow / dishLabel / dish / matchBox / matchHead / matchLabel / matchScore /
verdict / panman / panmanFace / bubble / panmanText / tierEmoji`

→ 適用フェーズで削除し、生きているトークンへ寄せる。
併せて、散在する生の色・余白リテラルを上記 `--ts-*` トークン（または共通定数）へ集約する。
**変更しないも**の: 相性ロジック（`calcMatch`）・判定の文言と閾値（`matchVerdict`/`verdictTier`）・パンマンのアニメ挙動。
